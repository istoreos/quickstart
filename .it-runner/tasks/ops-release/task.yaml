version: "1"
name: ops-release
description: Build quickstart release artifacts
tags: [ops, release]
workspace:
  workdir: ${PROJECT_ROOT}
logs:
  rootDir: .it-runner/logs
run:
  cmds:
    - make ops-release
