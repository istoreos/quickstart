version: "1"
name: ops-targets
description: List available quickstart deployment targets
tags: [ops, deploy]
workspace:
  workdir: ${PROJECT_ROOT}
logs:
  rootDir: .it-runner/logs
run:
  cmds:
    - make ops-targets
