version: "1"
name: ops-preflight-selected
description: Check SSH connectivity and remote quickstart prerequisites
tags: [ops, deploy, remote]
workspace:
  workdir: ${PROJECT_ROOT}
watch:
  stateFile: ${PROJECT_ROOT}/.it-runner/states/ops-preflight-selected.STATE
logs:
  rootDir: .it-runner/logs
run:
  cmds:
    - make ops-preflight-selected
